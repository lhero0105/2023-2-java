package com.green.day3.ch4;

public class FirstNumCatch {
    /*
    int score = 100;
    String opt = "";
    if (score >= 80) {
        int rMod = score % 10; // 첫번째 자리를 캐치하고 싶다? > % 10
        if (rMod >= 8 || score == 100){
            opt = "+";
        } else if (score % 10 <= 3) {
            opt = "-";
        }
    }

     */
}
